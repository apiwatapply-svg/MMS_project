{
  "_description": "Unified OEE calculation settings. Every machine uses the same status-based runtime, visual NG, runtime-based CT, and target deduction rules.",
  "default_mode": "status_based",
  "custom_modes": {},
  "ng_modes": {
    "default": "visual_ng"
  },
  "ct_calc_modes": {
    "default": "runtime_based"
  },
  "timezone_modes": {
    "default": "local"
  },
  "target_deduct_excluded": {
    "default": true
  },
  "availability_targets": {
    "default": "eff_target"
  }
}
